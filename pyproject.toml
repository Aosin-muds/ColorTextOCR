
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
name = "ColorTextOCR"
version = "0.0.5"
packages = find_packages()
install_requires = [
    "numpy",
    "opencv-python",
    "Pillow",
    "pytesseract"
]
